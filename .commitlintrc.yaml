extends:
  - "@commitlint/config-conventional"
rules:
  scope-enum:
    - 2
    - always
    - - api
      - web
      - database
      - services
      - trpc
      - logger
      - typescript-config
      - root